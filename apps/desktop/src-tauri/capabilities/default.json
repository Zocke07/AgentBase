{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The sidecar is spawned from Rust, not from JavaScript, so the webview needs no shell permission at all.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
